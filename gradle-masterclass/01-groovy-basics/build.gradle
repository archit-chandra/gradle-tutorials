import java.text.SimpleDateFormat

//System.out.println("Hello World!")
println 'Hello World!'

println '--------------------------------------------------------------------------'

SimpleDateFormat sdf = new SimpleDateFormat("yyyy-MM-dd'T'HH:mm:ss'Z'")
sdf.setTimeZone(TimeZone.getTimeZone("UTC"))

println "Hello World! " + sdf.format(new Date())


println '--------------------------------------------------------------------------'
// inline code example

// NOTES: Java vs Groovy
//  The method can also run before the method declaration
//  Groovy reads the whole script before it invokes any method
sayHelloWorld()

void sayHelloWorld() {
    println "Hello World again!"
}

sayHelloWorld()

println '--------------------------------------------------------------------------'

// NOTES: return statemenet
//   It is optional
//   When more than 1 line in method , the value of last line is returned by default

int doubleIt(int i){
    println i
    i * 3
    def y = 2 // last line value is returned, even if it is a declaration
}

println doubleIt(2)

println '--------------------------------------------------------------------------'

// NOTES: Groovy Types
//   It can be Typed/UnTyped for variables, parameters or return values
//   When untyped then use def (it is equivalent of Object in Java) - No Type safety and can get Runtime exception

def TripleIt(i){
    println i
    i * 3
    def y = 5
}

println TripleIt(3)

println '--------------------------------------------------------------------------'